# awsl real-provider fixture

For this directory, the project instruction marker is
`AWSL_PROJECT_INSTRUCTION_CLAUDE_V1`.

Read-only acceptance runs must not create, edit, delete, move, or rename files.
